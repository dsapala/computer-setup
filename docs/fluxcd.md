# FluxCD Version 0.25.2

```
brew tap fluxcd/homebrew-tap && pushd "$(brew --prefix)/Homebrew/Library/Taps/fluxcd/homebrew-tap" && git checkout e3bab3b1abb2f06e15a739e19260e41186334cea && brew install fluxcd/tap/flux && popd
```

